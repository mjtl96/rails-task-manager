<% @tasks.each do |task| %>
  <div>
    <%= link_to task.title, task_path(task.id) %>
    <%= task.details %>
    <% if task.completed == true %>
      Check
    <% else %>
      To Do
    <% end %>
  </div>
<% end %>
